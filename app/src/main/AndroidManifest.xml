<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.javademoapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JavaDemoApp">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".subMainActivity" />
        <activity android:name=".subMainActivity2" />
        <activity
            android:name=".subMainActivity3"
            android:exported="false" />
        <activity android:name=".subMainActivity4" />
        <activity android:name=".subMainActivity5" />
        <activity android:name=".subMainActivity6" />
    </application>

</manifest>